// process.once('loaded', () => {
//   // process is loaded, now we load the webpage
// })

// window.onload = () => {
//   // page is now loaded
// }

require('./dist/bundle') // inject the parcel generated bundle
